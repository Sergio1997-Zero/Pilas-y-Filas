class pilas
{
    constructor()
    {        
        this.numeros = [7,3,2,6,5,8] 
        this.pares = []                      
    }

    Pares()
    {
        for(let x = 0; x < this.numeros.length;x++)
        {
            if(this.numeros[x] % 2 == 0)
            {
                this.pares.push(this.numeros[x])
            }
        }
        console.log(this.numeros)
        console.log(this.pares)
    }  
    
}
let pilas1 = new pilas
pila1.Pares()